<div id="fh5co-product">
    <div class="container">
        <div class="row animate-box">
            <div class="col-md-8 col-md-offset-2 text-center fh5co-heading">
                <h4>My Recipes</h4>
                <p class="mt-40">
                    <a href="/dashboard/new" class="btn btn-primary btn-outline btn-lg">Add a Recipe</a>
                </p>
            </div>
        </div>
        <div class="row">
            {{#each recipes as |recipe|}}
            {{> my-recipe-info recipe }}
            {{/each}}
        </div>
    </div>
</div>